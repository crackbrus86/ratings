import * as React from "react";
import * as ReactDOM from "react-dom";

import Layout from "./Layout";

const RatingsOptionsApp: React.FC = () => {
  return <Layout />;
};

ReactDOM.render(
  <RatingsOptionsApp />,
  document.getElementById("app-rat-options")
);
